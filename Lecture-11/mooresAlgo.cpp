#include <iostream>
#include <vector>
using namespace std;

int mooreAlgo(vector<int> arr) {

    int n = arr.size();
    
};



int main() {
    

    // moore's algo do not need to sort the array

    vector<int> arr = {1,2,2,1,1};
    return 0;
}